chrome.dir = ${user.dir}\\chrome-profiles
chrome.profile.dir = profileForTests

# From active profile
profile.id=${profile.id}
browser.headless=${browser.headless}
use.chrome.profile=${use.chrome.profile}
use.selenoid=${use.selenoid}